@page "/pressureReadings"

@using BlazorApp.Data
@using Domain.Models

<h1>Pressure Readings</h1>

<p>This component demonstrates fetching data from a service.</p>

@if (service == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <button class="btn btn-primary" @onclick="service.AddReading">Generate pressure reading</button>
    <button class="btn btn-primary" @onclick="service.ClearReadings">Clear pressure readings</button>
    <table class="table">
        <thead>
            <tr>
                <th>Raw value</th>
                <th>BAR</th>
                <th>PSI</th>
                <th>Timestamp</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var reading in service.GetReadings())
            {
                <tr>
                    <td>@reading.RawValue</td>
                    <td>@reading.BAR</td>
                    <td>@reading.PSI</td>
                    <td>@reading.TimeStamp</td>
                </tr>
            }
        </tbody>
    </table>
}

@code {
    PressureReadingService service = PressureReadingSingleton.GetService();
}
